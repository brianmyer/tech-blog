
<div id="blogPosts">

{{#each blogPosts as |blogPost|}}

<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/api/blogPosts/{{blogPost.id}}">{{blogPost.title}}</a>
    </h2>
    
    <p>Created by {{blogPost.user.name}} on {{format_date blogPost.date_created}}</p>
  </div>
</div>
{{/each}}


</div>
